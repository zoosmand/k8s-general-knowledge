apiVersion: v1
kind: Service
metadata:
  name: {{ include "sb-10.fullname" . }}
  labels:
    {{- include "sb-10.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sb-10.selectorLabels" . | nindent 4 }}

NAME: sb-12
LAST DEPLOYED: Thu Oct 12 12:56:19 2023
NAMESPACE: default
STATUS: pending-install
REVISION: 1
TEST SUITE: None
HOOKS:
MANIFEST:
---
# Source: ips-monitoring/templates/00-namespace.yaml
apiVersion: v1
kind: Namespace
metadata:
  name: sb-11-monitoring
---
# Source: ips-monitoring/templates/01-serviceaccount.yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  name: sb-11-sa
  namespace: sb-11-monitoring
  labels:
    helm.sh/chart: ips-monitoring-0.0.1
    app: sb-11
    instance: sb-12
    app.kubernetes.io/version: "1.16.0"
    app.kubernetes.io/managed-by: Helm
automountServiceAccountToken: true
---
# Source: ips-monitoring/templates/10-configmap.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-config
  namespace: sb-11-monitoring
  labels:
    helm.sh/chart: ips-monitoring-0.0.1
    app: sb-11
    instance: sb-12
    app.kubernetes.io/version: "1.16.0"
    app.kubernetes.io/managed-by: Helm
immutable: true
data:
  #
  prometheus.yml: |+
    global:
      scrape_interval: 15s
      evaluation_interval: 25s

    #alerting:
    #  alertmanagers:
    #    - static_configs:
    #        - targets:
    #          - alertmanager:9093

    rule_files:
      # - "first_rules.yml"
      # - "second_rules.yml"

    scrape_configs:
      - job_name: "Prometheus"
        static_configs:
          - targets: ["localhost:9090"]
      # - job_name: "Pushgateway"
      #   honor_labels: true
      #   static_configs:
      #   - targets: ["pushgateway-srv-9091.sb-11-monitoring.svc.cluster.local:9091"]
---
# Source: ips-monitoring/templates/02-hardcoded-pv.yaml
apiVersion: v1
kind: PersistentVolume
metadata:
  name: m8-local-storage-monitoring
  labels:
    file-system: ext4
    physical-volume: lvm-partition
    max-size: 20Gi
    helm.sh/chart: ips-monitoring-0.0.1
    app: sb-11
    instance: sb-12
    app.kubernetes.io/version: "1.16.0"
    app.kubernetes.io/managed-by: Helm
spec:
  capacity:
    storage: 20Gi
  volumeMode: Filesystem
  accessModes:
  - ReadWriteOnce
  persistentVolumeReclaimPolicy: Delete
  storageClassName: node0-local-storage-monitoring
  local:
    path: /home/zoosman/dev/k0s/volumes/mnt/monitoring
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: In
          values:
          - mp8
---
# Source: ips-monitoring/templates/11-prometheus-pvc.yaml
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: monitoring-data
  namespace: sb-11-monitoring
  labels:
    helm.sh/chart: ips-monitoring-0.0.1
    app: sb-11
    instance: sb-12
    app.kubernetes.io/version: "1.16.0"
    app.kubernetes.io/managed-by: Helm
spec:
  selector:
    matchLabels:
      app: sb-11
  storageClassName: node0-local-storage-monitoring
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
---
# Source: ips-monitoring/templates/13-prometheus-srv.yaml
apiVersion: v1
kind: Service
metadata:
  name: prometheus-srv-9090
  namespace: sb-11-monitoring
  labels:
    helm.sh/chart: ips-monitoring-0.0.1
    app: sb-11
    instance: sb-12
    app.kubernetes.io/version: "1.16.0"
    app.kubernetes.io/managed-by: Helm
spec:
  selector:
    app: sb-11
  type: LoadBalancer
  loadBalancerIP: 10.203.10.25
  ports:
    - protocol: TCP
      name: http-9090
      port: 9090
      targetPort: 9090
---
# Source: ips-monitoring/templates/21-pushgatewasy-srv.yaml
apiVersion: v1
kind: Service
metadata:
  name: pushgateway-srv-9091
  namespace: sb-11-monitoring
  labels:
    helm.sh/chart: ips-monitoring-0.0.1
    app: sb-11
    instance: sb-12
    app.kubernetes.io/version: "1.16.0"
    app.kubernetes.io/managed-by: Helm
spec:
  selector:
    app: sb-11
  type: LoadBalancer
  loadBalancerIP: 10.203.10.26
  ports:
    - protocol: TCP
      name: http-9091
      port: 9091
      targetPort: 9091
---
# Source: ips-monitoring/templates/12-prometheus-statfullset.yaml
apiVersion: apps/v1
kind: StatefulSet
metadata:
  labels:
    helm.sh/chart: ips-monitoring-0.0.1
    app: sb-11
    instance: sb-12
    app.kubernetes.io/version: "1.16.0"
    app.kubernetes.io/managed-by: Helm
    test: liveness
  name: prometheus-sb-11-ss
  namespace: sb-11-monitoring
spec:
  serviceName: prometheus-sb-11-ss-srv
  replicas: 1
  selector:
    matchLabels:
      app: sb-11
  template:
    metadata:
      labels:
        app: sb-11
        instance: sb-12
    spec:
      nodeSelector:
        node-type: worker
      restartPolicy: Always
      serviceAccountName: sb-11-sa
      automountServiceAccountToken: true
      containers:
        # ---
        - name: prometheus
          image: reg.docker.askug.net:5000/prometheus-sb-11:0.0.0

          # command: [ "nohup", "/bin/sh", "-c" ]
          # args:
          #   ["while true; do rm -f /tmp/healthy; date > /tmp/healthy; sleep 7; done", "&"]
          #   # - touch /tmp/healthy; sleep 30; rm -f /tmp/healthy; sleep 600
          
          livenessProbe:
            httpGet:
              path: /-/healthy
              port: 9090
              httpHeaders:
              - name: Authorization
                value: "Bearer TokenXXXToken" # ToDo put the token to a secret
            initialDelaySeconds: 5
            periodSeconds: 5

          readinessProbe:
            httpGet:
              path: /-/ready
              port: 9090
              httpHeaders:
              - name: Authorization
                value: "Bearer TokenXXXToken" # ToDo put the token to a secret
            initialDelaySeconds: 3
            periodSeconds: 3

          ports:
          - containerPort: 9090
            name: tcp-9090

          env:
          - name: PROMETHEUS_VERSION
            value: 2.47.0 # ToDo remove verison into vars

          resources:
            limits:
              cpu: "4"
              memory: 4Gi
            requests:
              cpu: "1"
              memory: 512Mi

          volumeMounts:
            - mountPath: "/etc/prometheus/prometheus.yml"
              name: monitoring-data-config
              subPath: "prometheus.yml"
              readOnly: true
            - mountPath: "/var/lib/prometheus"
              name: monitoring-data-volume
              subPath: "caches/prometheus"

      volumes:
        - name: monitoring-data-config
          configMap:
            name: prometheus-config
        - name: monitoring-data-volume
          persistentVolumeClaim:
            claimName: monitoring-data
---
# Source: ips-monitoring/templates/20-pushgateway-statfullset.yaml
apiVersion: apps/v1
kind: StatefulSet
metadata:
  labels:
    helm.sh/chart: ips-monitoring-0.0.1
    app: sb-11
    instance: sb-12
    app.kubernetes.io/version: "1.16.0"
    app.kubernetes.io/managed-by: Helm
    test: liveness
  name: pushgateway-sb-11-ss
  namespace: sb-11-monitoring
spec:
  serviceName: pushgateway-sb-11-ss-srv
  replicas: 1
  selector:
    matchLabels:
      app: sb-11
  template:
    metadata:
      labels:
        app: sb-11
        instance: sb-12
    spec:
      nodeSelector:
        node-type: worker
      restartPolicy: Always
      serviceAccountName: sb-11-sa
      automountServiceAccountToken: true
      containers:
        # ---
        - name: pushgateway
          image: reg.docker.askug.net:5000/pushgateway-sb-11:0.0.0

          # command: [ "nohup", "/bin/sh", "-c" ]
          # args:
          #   ["while true; do rm -f /tmp/healthy; date > /tmp/healthy; sleep 7; done", "&"]
          #   # - touch /tmp/healthy; sleep 30; rm -f /tmp/healthy; sleep 600
          
          livenessProbe:
            httpGet:
              path: /-/healthy
              port: 9091
            initialDelaySeconds: 5
            periodSeconds: 5

          readinessProbe:
            httpGet:
              path: /-/ready
              port: 9091
            initialDelaySeconds: 3
            periodSeconds: 3

          ports:
          - containerPort: 9091
            name: tcp-9091

          env:
          - name: PUSHGATEWAY_VERSION
            value: 1.6.2 # ToDo remove verison into vars

          resources:
            limits:
              cpu: "1"
              memory: 512Mi
            requests:
              cpu: 500m
              memory: 256Mi

NOTES:
ips-monitoring
sb-11
kubectl --namespace sb-11-monitoring
